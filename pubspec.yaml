name: custom_rich_text
description: A custom rich text package that allow highlights and link reference to text.
version: 0.0.1
homepage: https://github.com/Mindinventory/custom_rich_text/tree/master

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
